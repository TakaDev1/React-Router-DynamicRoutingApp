# 課題

### 問題文

ユーザーIDに応じて異なる情報を表示するページを作成してください。

ルートは `/user/:id` とし、URLのパラメータからIDを取得して表示します。

### 条件

1. `useParams` を使用する
2. `/user/:id` のルートでIDを表示する
3. IDは `text-xl font-semibold text-blue-600`
4. TypeScriptで作成する